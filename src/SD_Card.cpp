#include "SD_Card.hpp"

// SD Card

//#define CS_pin 4
//#define File_Name "data.csv"
//File myFile;
// RTC_DS1307 Clock;
// DateTime now = Clock.now();

//int rev = 0;

void Write_SD_Card(int *lumin, float *hygr, float *pressure, float *temp)
{ //
    // SD.begin(SPI_HALF_SPEED, CS_pin);
    // String File_Name = rtc.now().toString("AAMMDD") + String("_") + rev + ".log";
    // if(SD.exists(File_Name)){
    //     rev += 1;
    // }
	// myFile = SD.open(File_Name, FILE_WRITE);
    // String donnee = rtc.now().toString("DD:AA:MM hh:mm:ss : ") + String(*lumin) + String(*hygr) + String(*pressure) + String(*temp);
    // myFile.println(donnee);
    // if(myFile.size() >= fetch_eeprom_data(FILE_MAX_SIZE.adr)){
    //     Serial.print("taille trop grande");
    // }
    // else{
    //     Serial.print("taille bien");
    // }
    // myFile.close();



}